int firstElementKTime(int a[], int n, int k)
    {
         map<int,int> mapp;
        for(int i=0;i<n;i++)
        {
            mapp[a[i]]++;
            if(mapp[a[i]]==k)
            {
                return a[i];
            }
        }
        return -1;
    }
